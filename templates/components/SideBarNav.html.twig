<div{{attributes}}>
	{# =========================
	       SIDEBAR
	    ========================== #}
	<aside class="w-64 h-full bg-white border-r border-gray-200 flex flex-col justify-between">

		<div>

			{# Logo #}
			<div class="p-6 flex items-center gap-3">
				<img src="{{ asset('Icon.png') }}" alt="Logo" class="w-8 h-8">

				<span class="text-2xl font-bold text-blue-600">
					FacturSaaS
				</span>
			</div>

			{# Navigation #}
			<nav class="px-4 space-y-2">

				<a href="{{ path('app_dashboard') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl bg-blue-50 text-blue-600 font-medium">

					<span>📊</span>
					Dashboard
				</a>

				<a href="{{ path('app_invoice_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-gray-100 text-gray-700">

					<span>📄</span>
					Factures
				</a>

				<a href="{{ path('app_client_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-gray-100 text-gray-700">

					<span>👤</span>
					Clients
				</a>

				<a href="{{ path('app_product_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-gray-100 text-gray-700">

					<span>📦</span>
					Produits
				</a>

				<a href="#" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-gray-100 text-gray-700">

					<span>⚙️</span>
					Profil
				</a>

			</nav>
		</div>
	</div>
